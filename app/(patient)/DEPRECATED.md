# DEPRECATED — Patient Portal

This portal (`app/(patient)/`) is **deprecated** in `onwynd-dashboard`.

The patient-facing experience lives in the `web/` folder (the MVP web app).

**Do not extend this portal.** New patient features go in `web/`.

This directory is kept temporarily to avoid breaking existing routes during transition.
Once the web portal has full feature parity, this directory should be deleted.
